# The user enters the string S. Program that replacing all colons (:) in a string with semicolons (;). Also
# count the number of replacements and print the answer to the screen (and a new line too).

text = []
user_text = input('Enter text: ')
text.append(user_text)

#print(f'Replaced text: {user_text} \nNumber of changes: {count}')